{
  "extends": "./tsconfig.base.json",
  "//": "Single-pass type check over the workspace. As the monorepo grows this can move to a solution-style config with per-package `references`.",
  "compilerOptions": {
    "noEmit": true,
    "types": ["vitest/globals", "node"]
  },
  "include": ["tests/**/*.ts", "packages/**/src/**/*.ts"]
}
